<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectP"
        tools:targetApi="31">

        <activity
            android:name=".agentReportInterface"
            android:exported="false" />
        <activity
            android:name=".government_login"
            android:exported="false" />
        <activity
            android:name=".pharmacyInterface"
            android:exported="false" />
        <activity
            android:name=".ph_registration"
            android:exported="false" />
        <activity
            android:name=".pharmacy_login"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false"/>

        <activity
            android:name=".suplash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>